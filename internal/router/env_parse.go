package router

// env_parse.go previously held small helpers to parse MEDIA_* environment variables.
// Those helpers were moved to internal/utils/env.go so they can be reused in other internal packages.
